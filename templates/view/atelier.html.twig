{% extends 'base.html.twig' %}

{% block title %}!{% endblock %}
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="example-wrapper">
    <div class="text-center mb-4">
    <h1 class="h3 mb-3 font-weight-normal">Gestion Des Ateliers</h1>
    </div>
    <form action= "{{ path('ajoutAtelier') }} "method= "POST" >
        <a href="{{path('ajoutAtelier')}}" type="button" class="btn btn-link">Ajouter un Atelier</button></a>
    <div class="table-responsive-sm">
    <table class="table table-bordered">
    <thead class="thead-light">
    <tr>
        <th scope="col">Id</th>
        <th scope="col">Description</th>
        <th scope="col">Statut</th>
        <th scope="col">Type</th>
        <th scope="col">Prix</th>
    </tr>
    </thead>
    <tbody>
    <tr>
    {%for atelier in lesAteliers%}
        <th scope="row">{{atelier.id}}</th>
        <td>{{atelier.description}}</td>
        <td>{{atelier.statut}}</td>
        <td>{{atelier.type}}</td>
        <td>{{atelier.prix}}</td>
    </tr>
    </div>
    {%endfor%}
</div>
{% endblock %}